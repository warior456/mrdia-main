module.exports = {
    name: 'owner',
    aliases: ['**======================**'],
    description: '**======================**',

    run: async(message, client, Discord, args, cmd) => {
        return
    }
}