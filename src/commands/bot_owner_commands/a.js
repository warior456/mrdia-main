module.exports = {
    name: 'owner',
    aliases: ['**======================**'],
    description: '**======================**',

    run: async(message, args, cmd, client, Discord) => {
        return
    }
}