module.exports = {
    name: 'music',
    aliases: ['**======================**'],
    description: '**======================**',

    run: async(message, args, cmd, client, Discord) => {
        return
    }
}