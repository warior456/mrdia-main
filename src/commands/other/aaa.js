module.exports = {
    name: 'other',
    aliases: ['**======================**'],
    description: '**======================**',

    run: async(message, client, Discord, args, cmd) => {
        return
    }
}