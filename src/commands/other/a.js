module.exports = {
    name: 'other',
    aliases: ['**======================**'],
    description: '**======================**',

    run: async(message, args, cmd, client, Discord) => {
        return
    }
}